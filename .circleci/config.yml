version: 2.1

jobs:
  lint:
    docker:
      - image: cimg/python:3.12
    steps:
      - checkout
      - run: echo "lint ok"

  test:
    docker:
      - image: cimg/python:3.12
    steps:
      - checkout
      - run: echo "tests ok"

  build:
    docker:
      - image: cimg/python:3.12
    steps:
      - checkout
      - run: echo "build ok"

workflows:
  version: 2
  pipeline:
    jobs:
      - lint
      - test
      - build
